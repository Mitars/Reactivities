name: CI/CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run a one-line script
      run: |
        echo $CANDY_SECURE
      env:
        CANDY_SECURE: ${{ secrets.DIGITALOCEAN_PASSWORD }}
